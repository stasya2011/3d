export interface IExperiences {
  title: string;
  company_name: string;
  icon: string;
  iconBg: string;
  date: string;
  points: string[];
}

export interface IProject {
  iconUrl: string;
  theme: string;
  name: string;
  description: string;
  link: string;
}
